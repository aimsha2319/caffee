{
  "name": "@caffee/types",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint 'src/**/*.ts'",
    "test": "echo 'No tests yet'",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "3.22.4"
  },
  "devDependencies": {
    "rimraf": "5.0.5"
  }
}
